# 🏦 Банковское приложение "БанкМД" — Микросервисная архитектура

![Java](https://img.shields.io/badge/Java-17-orange)
![Spring Boot](https://img.shields.io/badge/Spring%20Boot-2.7.5-brightgreen)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-blue)
![Microservices](https://img.shields.io/badge/Architecture-Microservices-important)

## 📖 Оглавление

- [Обзор проекта](#-обзор-проекта)
- [Архитектура](#-архитектура)
- [Технологический стек](#-технологический-стек)
- [Микросервисы](#-микросервисы)
- [Структура базы данных](#-структура-базы-данных)
- [Быстрый старт](#-быстрый-старт)
- [API Документация](#-api-документация)
- [Безопасность](#-безопасность)
- [Разработка](#-разработка)
- [Тестирование](#-тестирование)
- [Деплоймент](#-деплоймент)

---

## 🎯 Обзор проекта

**Мета-Банк** — это современное банковское приложение, построенное на микросервисной архитектуре. Проект реализует полный цикл банковских операций: от управления клиентскими профилями и счетами до обработки транзакций и противодействия мошенничеству.

### 🎯 Основные возможности

- ✅ Управление клиентскими профилями и документами
- ✅ Открытие и ведение банковских счетов  
- ✅ Выполнение переводов (по телефону, карте, счету)
- ✅ Система обнаружения мошеннических операций
- ✅ Полная история операций и аудит
- ✅ Публичная информация о банке и отделениях
- ✅ Ролевая система доступа с Spring Security
- ✅ Service Discovery с Eureka

---

## 🏗️ Архитектура

### Микросервисная структура
parent/ (корневой POM)
├── 📱 profile/ - Профили клиентов
├── 💳 account/ - Банковские счета
├── 🔄 transfer/ - Переводы и транзакции
├── 🕵️ antifraud/ - Антифрод система
├── 📊 history/ - История операций
├── 🏢 public-info/ - Публичная информация
├── 🔐 authorization/ - Аутентификация
└── 🌐 eureka-service/ - Service Discovery

### Схема взаимодействия
Клиент → API Gateway → [Микросервисы] ←→ Eureka Server
↓
PostgreSQL (схемы)


---

## ⚙️ Технологический стек

### Бэкенд
- **Java 17** - основной язык разработки
- **Spring Boot 2.7.5** - фреймворк для микросервисов
- **Spring Cloud** - инструменты для микросервисной архитектуры
- **Spring Security** - аутентификация и авторизация
- **Spring Data JPA** - работа с базой данных

### База данных
- **PostgreSQL 15** - основная СУБД
- **Liquibase** - управление миграциями БД
- **Hibernate** - ORM

### Вспомогательные технологии
- **MapStruct** - маппинг DTO/Entity
- **Lombok** - сокращение boilerplate кода
- **OpenFeign** - HTTP клиент для межсервисного взаимодействия
- **Springdoc OpenAPI** - документация API
- **Docker** - контейнеризация

### Тестирование
- **JUnit 5** - модульное тестирование
- **Mockito** - мокирование зависимостей
- **JaCoCo** - покрытие кода тестами

---

## 🔧 Микросервисы

### 1. 🆔 **Профиль клиента** (`profile`)
**Назначение**: Управление персональными данными клиентов
- Основная информация клиента
- Паспортные данные
- Адреса регистрации
- Аудит изменений профиля

**Схема БД**: `profile`

### 2. 💰 **Банковские счета** (`account`)
**Назначение**: Управление банковскими счетами
- Реквизиты счетов
- Балансы и лимиты
- История изменений счетов

**Схема БД**: `account`

### 3. 🔄 **Переводы** (`transfer`)
**Назначение**: Обработка финансовых операций
- Переводы по номеру телефона
- Переводы по номеру карты  
- Переводы по номеру счета
- Аудит всех транзакций

**Схема БД**: `transfer`

### 4. 🕵️ **Антифрод система** (`antifraud`)
**Назначение**: Обнаружение подозрительных операций
- Мониторинг переводов
- Блокировка подозрительных операций
- Анализ паттернов мошенничества

**Схема БД**: `anti_fraud`

### 5. 📊 **История операций** (`history`)
**Назначение**: Агрегация и хранение истории
- Сбор аудит-логов со всех микросервисов
- Предоставление полной истории операций
- Аналитика и отчетность

**Схема БД**: `history`

### 6. 🏢 **Публичная информация** (`public-info`)
**Назначение**: Информация о банке для клиентов
- Данные о банке и филиалах
- Информация о банкоматах
- Лицензии и сертификаты

**Схема БД**: `public_bank_information`

### 7. 🔐 **Авторизация** (`authorization`)
**Назначение**: Управление доступом и безопасностью
- Аутентификация пользователей
- Ролевая модель доступа
- Session-based безопасность

**Схема БД**: `auth`

### 8. 🌐 **Service Discovery** (`eureka-service`)
**Назначение**: Регистрация и обнаружение микросервисов
- Централизованный реестр сервисов
- Балансировка нагрузки
- Мониторинг состояния сервисов

---

## 🗃️ Структура базы данных
---

### Схемы PostgreSQL

```sql
-- Основные схемы приложения
CREATE SCHEMA auth;                    -- Аутентификация и роли
CREATE SCHEMA profile;                 -- Профили клиентов
CREATE SCHEMA account;                 -- Банковские счета
CREATE SCHEMA transfer;                -- Переводы
CREATE SCHEMA anti_fraud;              -- Антифрод система
CREATE SCHEMA history;                 -- История операций
CREATE SCHEMA public_bank_information; -- Публичная информация
```
### Ключевые таблицы
👤 **Профиль клиента**
- profile.profile - основной профиль
- profile.passport - паспортные данные
- profile.actual_registration - адрес проживания
- profile.audit - аудит изменений

💳 **Счета**
- account.account_details - реквизиты счетов
- account.account - метаданные счетов
- account.audit - история изменений

🔄 **Переводы**
- transfer.phone_transfer - переводы по телефону
- transfer.card_transfer - переводы по карте
- transfer.account_transfer - переводы по счету
- transfer.audit - аудит транзакций

🕵️ **Антифрод**
- anti_fraud.suspicious_*_transfer - подозрительные операции
- anti_fraud.audit - аудит антифрод системы


🚀 **Быстрый старт**
Предварительные требования
- Java 17 или выше
- Maven 3.6+
- Docker и Docker Compose
- PostgreSQL 15

#1. 🐳 Запуск базы данных

## Запуск PostgreSQL в Docker
```bash
docker run --name bank-postgres \
  -e POSTGRES_PASSWORD=password \
  -e POSTGRES_USER=user \
  -d -p 5433:5432 postgres:15
```
## Создание схем
```java
docker exec -it bank-postgres psql -U user -c "
CREATE SCHEMA auth;
CREATE SCHEMA profile; 
CREATE SCHEMA account;
CREATE SCHEMA transfer;
CREATE SCHEMA anti_fraud;
CREATE SCHEMA history;
CREATE SCHEMA public_bank_information;
"
```
#2. 📦 Сборка проекта
## Клонирование репозитория
```java
git clone <repository-url>
cd parent
```
## Сборка всех микросервисов
```java
mvn clean install
```
#3. 🎯 Запуск микросервисов
## Запуск Eureka Server (первым)
```java
mvn spring-boot:run -pl eureka-service
```
## Запуск остальных микросервисов (в отдельных терминалах)
```java
mvn spring-boot:run -pl profile
mvn spring-boot:run -pl account
mvn spring-boot:run -pl transfer
mvn spring-boot:run -pl authorization
```
## ... и так для всех микросервисов

#4. ✅ Проверка работоспособности
```java
Eureka Dashboard: http://localhost:8761

Swagger UI: http://localhost:8080/swagger-ui.html (для каждого микросервиса)

Health Check: http://localhost:8080/actuator/health
```
## Часть 5/7: API документация и безопасность


---

## 📚 API Документация

Каждый микросервис предоставляет документацию API через Swagger/OpenAPI:

```bash
# Пример для сервиса профилей
http://localhost:8081/swagger-ui.html

# Пример для сервиса счетов  
http://localhost:8082/swagger-ui.html
```

Примеры API эндпоинтов

👤 Профиль клиента

GET    /api/profiles/{id}          # Получить профиль
POST   /api/profiles              # Создать профиль
PUT    /api/profiles/{id}         # Обновить профиль
GET    /api/profiles/{id}/passport # Паспортные данные


💳 Счета
http
GET    /api/accounts/{id}         # Информация о счете
POST   /api/accounts              # Открыть новый счет
GET    /api/accounts/{id}/balance # Получить баланс
🔄 Переводы
http
POST   /api/transfers/phone       # Перевод по телефону
POST   /api/transfers/card        # Перевод по карте
POST   /api/transfers/account     # Перевод по счету
GET    /api/transfers/history     # История переводов
🔐 Безопасность
Ролевая модель
ROLE_CLIENT - Клиент банка (доступ к своим данным)

ROLE_OPERATOR - Оператор поддержки

ROLE_FRAUD_ANALYST - Аналитик антифрода

ROLE_ADMIN - Администратор системы

Конфигурация Spring Security
```java
@Configuration
public class SecurityConfig {
    
    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
        http
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/admin/**").hasRole("ADMIN")
                .requestMatchers("/fraud/**").hasAnyRole("FRAUD_ANALYST","ADMIN")
                .requestMatchers("/operator/**").hasAnyRole("OPERATOR","ADMIN")
                .requestMatchers("/client/**").hasAnyRole("CLIENT","ADMIN")
                .anyRequest().authenticated()
            )
            .formLogin(form -> form
                .loginPage("/login")
                .successHandler(roleBasedSuccessHandler)
            );
        return http.build();
    }
}
```

Панели доступа
Клиенты: /client - управление счетами и переводами

Операторы: /operator - поддержка клиентов

Аналитики: /fraud - мониторинг подозрительных операций

Администраторы: /admin - управление системой

## Часть 6/7: Разработка и тестирование

---

## 💻 Разработка

### Структура микросервиса
src/main/java/com/bank/[service]/
├── config/ # Конфигурационные классы
├── controller/ # REST контроллеры
├── service/ # Бизнес-логика
├── repository/ # Data Access Layer
├── entity/ # JPA сущности
├── dto/ # Data Transfer Objects
├── mapper/ # MapStruct мапперы
├── exception/ # Кастомные исключения
└── handler/ # Exception handlers


### Стандарты кода

- **Checkstyle** - проверка стиля кода
- **Lombok** - для сокращения boilerplate кода
- **MapStruct** - для маппинга между DTO и Entity
- **JUnit 5 + Mockito** - для тестирования

### Пример создания сущности

```java
@Entity
@Table(name = "profile", schema = "profile")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class Profile {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    private String nameOnCard;
    private String phoneNumber;
    private String email;
    
    @OneToOne
    @JoinColumn(name = "passport_id")
    private Passport passport;
}
```

🧪 Тестирование
Запуск тестов
```bash
# Все тесты
mvn test
```
# Тесты с покрытием
```bash
mvn jacoco:check test
```

# Тесты конкретного микросервиса
```bash
mvn test -pl profile
```
Стандарты тестирования
Покрытие кода: минимум 70%

Unit тесты: для сервисов и утилит

Integration тесты: для REST API

Mockito: для мокирования зависимостей

Пример теста
```java
@ExtendWith(MockitoExtension.class)
class ProfileServiceTest {
    
    @Mock
    private ProfileRepository profileRepository;
    
    @InjectMocks
    private ProfileService profileService;
    
    @Test
    void shouldReturnProfileWhenExists() {
        // given
        Profile profile = Profile.builder().id(1L).nameOnCard("TEST USER").build();
        when(profileRepository.findById(1L)).thenReturn(Optional.of(profile));
        
        // when
        ProfileDTO result = profileService.getProfile(1L);
        
        // then
        assertThat(result.getNameOnCard()).isEqualTo("TEST USER");
    }
}
```

## Часть 7/7: Деплоймент и заключение


---

## 🐳 Деплоймент

### Docker Compose (разработка)

```yaml
version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: bank
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  eureka-server:
    build: ./eureka-service
    ports:
      - "8761:8761"
    depends_on:
      - postgres
Конфигурация для продакшена
properties
# application-prod.properties
spring.datasource.url=jdbc:postgresql://prod-db:5432/bank
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka

logging.level.com.bank=INFO
management.endpoints.web.exposure.include=health,metrics,info
```

📊 Мониторинг и логирование
Spring Boot Actuator
Каждый микросервис предоставляет эндпоинты для мониторинга:

```http
GET /actuator/health     # Health check
GET /actuator/metrics    # Метрики приложения
GET /actuator/info       # Информация о сервисе
```
Prometheus Metrics
```xml
<dependency>
    <groupId>io.micrometer</groupId>
    <artifactId>micrometer-registry-prometheus</artifactId>
</dependency>
```
🤝 Участие в разработке
Процесс разработки
Создание задачи в таскборде проекта

Создание ветки от develop с номером задачи

Разработка с соблюдением стандартов кода

Тестирование с покрытием не менее 70%

Code Review командой разработки

Слияние в ветку develop

Требования к коммитам
Четкие сообщения коммитов на английском

Ссылка на номер задачи в коммите

Прохождение всех проверок CI/CD

📄 Лицензия
Этот проект является частью учебного курса и предназначен для образовательных целей.

👥 Команда проекта
Автор: Кывыржик А.Г.
Дата создания: 13.04.2023
Обновлено: 2024

📞 Поддержка
Для вопросов и предложений обращайтесь:

📧 Email: [alex.curly@mail.ru]

💬 Issues: [ссылка на Issues проекта]

📚 Документация: [ссылка на полную документацию]
